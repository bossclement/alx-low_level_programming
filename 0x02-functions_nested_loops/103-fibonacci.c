#include <stdio.h>
/**
 * main - hello
 * Description: world
 * Return: 0
 */
int main(void)
{
	int x = 0, h = 1, p = 0;
	int y = 0;

	while (next < 4000000)
	{
		next = x + h;
		x = h;
		h = p;
		if (p % 2 == 0)
			y += p;
	}
	printf("%i\n", sum);
	return (0);
}
